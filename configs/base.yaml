seed: 42
device: "auto"
num_workers: 0

paths:
  metadata_csv: "data/processed/brats_gli_2d/metadata.csv"
  output_dir: "outputs"
  checkpoints_dir: "outputs/checkpoints"
  figures_dir: "outputs/figures"
  metrics_dir: "outputs/metrics"
